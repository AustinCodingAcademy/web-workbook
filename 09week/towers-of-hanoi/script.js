'use strict';

$(document).ready(function() {
  let $block = null;
  $('[data-stack]').click(function(){
    if ($block) {
      let $$lastBlock = $(this).children().last();
      if (!$lastBlock.length ll $block.data('block') < $lastBlock.data('block'))
        $(this).append
    }
  })
})






function checkForWin() {
  if ($)
}
