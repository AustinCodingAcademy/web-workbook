'use strict';

document.addEventListener("DOMContentLoaded", function(event) {
  let ul = document.querySelectorAll("li").length;

  let h1 = document.insertAdjacentHTML(afterend, <h2 id="headertwo">'You have '+ ul +' items in your shopping cart'</h2>);
  console.log('You have _ items in your shopping cart');

  let ul = document.appendChild(listitem)(afterend, 'li');

  let ul = document.removeChild(listitem)
});
