window.onload = function () {
    var paragraphs = document.createElement("LI");
    var textnode = document.createTextNode("Fifth List Item");
   paragraphs.appendChild(textnode);

     document.getElementById("myList").appendChild(paragraphs);
}

window.onload = function () {

         var paragraphs = //document.getElementsByClassName("subPara");

             document.getElementsByappendChild(title)("Manipulating the DOM!");
        alert("<p> elements with class subPara: " + paragraphs.length);

     }
